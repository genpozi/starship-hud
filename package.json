{
  "name": "starship-mission-control",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "STELLARIS-7 — starship HUD mission control and multi-agent orchestration harness",
  "scripts": {
    "dev": "vite",
    "dev:server": "node --env-file-if-exists=.env server/index.js",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env server/index.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "express": "^4.21.1",
    "three": "^0.169.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
